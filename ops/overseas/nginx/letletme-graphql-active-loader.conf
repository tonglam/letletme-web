# Load the root-owned blue/green selector at Nginx http scope. The selector
# symlink and its blue/green targets are installed and switched only by
# letletme-vps-ops; this file must never contain a fixed GraphQL port.
include /etc/nginx/snippets/letletme-graphql-active.conf;
